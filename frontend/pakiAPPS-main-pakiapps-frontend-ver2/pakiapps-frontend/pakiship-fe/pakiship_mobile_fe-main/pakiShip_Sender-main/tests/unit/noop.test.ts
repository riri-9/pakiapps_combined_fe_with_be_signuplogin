/// <reference types="jest" />

// Placeholder unit test — replace with real feature tests as the project grows.
describe('noop', () => {
  it('passes', () => {
    expect(true).toBe(true);
  });
});
